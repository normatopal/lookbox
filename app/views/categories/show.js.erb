$("#modal-form").modal('hide');
$("#category-modal").html("<%= escape_javascript (render(partial: 'categories/modals/show', :locals => { category: @category } )) %>");
$("#category-modal").modal('show');

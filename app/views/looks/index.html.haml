#content
  .row
    .col-md-6
      -#%h2 Looks
    .col-md-6
      = link_to 'Add New Look', new_look_path, class: "btn btn-add-new"

  = render "tabs_links"

  = render "looks_list", is_shared_looks:  is_shared_looks ||= false

